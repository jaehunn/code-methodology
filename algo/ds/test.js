import LinkedList from "./linkedList";
import Stack from "./stack";
import queue from "./queue";

describe("Data Structure Test", () => {
  beforeEach(() => {
    const data = {
      numItems: [5, 4, 3, 2, 1],
      strItems: ["a", "c", "e", "g", "i"],
      objItems: [{ a: 1 }, { b: 2 }, { c: 3 }, { d: 4 }, { e: 5 }],
    };
  });

  describe("연결 리스트", () => {
    describe("연결 리스트 노드", () => {});
  });

  describe("스택", () => {});

  describe("큐", () => {});

  describe("해시 테이블", () => {});

  describe("힙", () => {});
});
