/* 
    이미 구현되어 있는 API 호출 함수
    apis.fetchToken(userId => Promise); 0.15s
    apis.fetchTossPoint(token => Promise); 0.6s
    apis.fetchTossMoney(token => Promise);  0.6s
*/

/**
 * @param {number} id
 * @return {Promise}
 */

const fetchTotalAmount = () => {
  return;
};
